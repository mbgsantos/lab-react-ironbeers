import { Link } from "react-router-dom";

const Navbar = () => {
    return (
        <nav className="navbar Navigator">
            <Link to='/'>Home</Link>
        </nav>
    );
};

export default Navbar;